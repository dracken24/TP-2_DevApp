<Page x:Class="TP_2.Pages.AccueilPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Accueil" Height="805">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="600"/>
        </Grid.RowDefinitions>

        <!-- En-tête de la page -->
        <Border Grid.Row="0" Background="#007ACC" Padding="30,20">
            <StackPanel>
                <TextBlock Text="🏠 Bienvenue" 
                           FontSize="32" 
                           FontWeight="Bold" 
                           Foreground="White"
                           HorizontalAlignment="Center"/>
                <TextBlock Text="Application Bureau - Page d'accueil" 
                           FontSize="16" 
                           Foreground="#E0E0E0"
                           HorizontalAlignment="Center"
                           Margin="0,10,0,0"/>
            </StackPanel>
        </Border>

        <!-- Contenu principal -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="40">

                <!-- Section de présentation -->
                <Border Background="#F8F9FA" CornerRadius="12" Padding="30">
                    <StackPanel>
                        <TextBlock Text="À propos de cette application" 
                                   FontSize="24" 
                                   FontWeight="Bold" 
                                   Foreground="#333333"
                                   Margin="0,0,0,15"/>
                        <TextBlock Text="Cette application WPF, dans le cadre du TP-2 du cours d'application de bureau."
                                   FontSize="16" 
                                   Foreground="#666666"
                                   TextWrapping="Wrap"
                                   LineHeight="24"/>
                    </StackPanel>
                </Border>

                <!-- Cartes de fonctionnalités -->
                <TextBlock Text="Fonctionnalités principales" 
                           FontSize="20" 
                           FontWeight="Bold" 
                           Foreground="#333333"
                           Margin="0,20,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="600"/>
                        <ColumnDefinition Width="0"/>
                    </Grid.ColumnDefinitions>

                    <!-- Carte 1 -->
                    <Border Grid.Column="0" 
                            Background="White" 
                            BorderBrush="#E0E0E0" 
                            BorderThickness="1" 
                            CornerRadius="8" 
                            Padding="25"
                            Margin="0,0,-103,0" Grid.ColumnSpan="2">
                        <StackPanel>
                            <TextBlock Text="⚙️" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,15"/>
                            <TextBlock Text="Fonctionnalités" 
                                       FontSize="18" 
                                       FontWeight="Bold" 
                                       Foreground="#333333"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,10"/>
                            <TextBlock
                                       FontSize="14" 
                                       Foreground="#666666"
                                       TextWrapping="Wrap"
                                       TextAlignment="Center" Height="77"><Run Text="Permet de creer ou supprimer des utilisateurs. Ajouter, modifier ou supprimer des evenements sur"/><LineBreak/><Run Text="le calendrier."/><LineBreak/><Run Text="Les evenements sont supprimer en cascade lors de la supression d'un utilisateur."/></TextBlock>
                        </StackPanel>
                    </Border>

                    <!-- Carte 2 -->
                </Grid>

                <!-- Section d'information -->

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
